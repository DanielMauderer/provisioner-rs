[package]
name = "basic"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
provisioner = { path = "../../crates/provisioner", features = ["esp32c3"] }
heapless = "0.9"
